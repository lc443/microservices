## pushing microservices images to docker 
run mvn clean compile jib:build 

## pulling images from hub using  docker compose

docker compose up -d

## running kafka locally 
 run this in terminal use command docker-compose up -d